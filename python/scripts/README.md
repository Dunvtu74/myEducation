# scripts

Small utility scripts.

## file_organizer.py

Sorts files in a folder into subdirectories by type: images, video, audio, docs, code, archives, other.

```bash
python file_organizer.py /path/to/folder
```

Run without arguments to organize the current directory.
