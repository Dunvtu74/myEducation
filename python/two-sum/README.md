# two-sum

Найти два числа в массиве, которые в сумме дают target. Вернуть их индексы.

```bash
python main.py
```

Задача #1 на leetcode. Наивное решение O(n²), это решение через хэш-таблицу — O(n).
Спрашивают везде и всегда.
