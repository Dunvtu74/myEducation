# fib-fast

Числа Фибоначчи без рекурсии. Классическая задача, которую спрашивают почти на каждом собесе на джуна.

```bash
python main.py 20
```

Рекурсивный вариант красивее выглядит, но падает на больших числах. Этот не падает.
