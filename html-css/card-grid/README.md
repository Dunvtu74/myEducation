# card-grid

Адаптивная сетка карточек на CSS Grid. Сама перестраивается под ширину экрана.

Просто открой `index.html` в браузере. Потяни окно по ширине — увидишь как карточки перестраиваются.

Задание из раздела по CSS Grid на курсах вёрстки.
