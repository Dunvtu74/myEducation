# palindrome-check

Проверяет строку на палиндром. Игнорирует пробелы, пунктуацию и регистр.

```bash
node index.js
```

Классика собесов на джуна. Решается в одну строку через reverse, но здесь сделано понятно.
