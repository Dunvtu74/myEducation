# throttle

Троттл — вызывает функцию не чаще чем раз в N миллисекунд.

```bash
node index.js
```

Дебаунс и троттл часто спрашивают вместе. Дебаунс ждёт паузы, троттл ограничивает частоту.
Нужен для обработки скролла, resize, mousemove.
