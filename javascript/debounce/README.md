# debounce

Дебаунс — одна из самых используемых утилит во фронтенде. Нужна когда не хочешь делать запрос на каждое нажатие клавиши.

```bash
node index.js
```

Работодатели любят спрашивать "напиши debounce с нуля" — теперь есть что показать.
